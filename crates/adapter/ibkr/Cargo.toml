[package]
name = "oath-adapter-ibkr"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

# cargo-machete: deps declared ahead of first use; prune from `ignored` as they
# are adopted (serde/serde_json/thiserror land in the cpapi wire/error modules).
[package.metadata.cargo-machete]
ignored = ["serde", "serde_json", "thiserror"]
