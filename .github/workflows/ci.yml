# CI workflow — runs on every push to main and on every pull request.
# Full reference: https://docs.github.com/en/actions/writing-workflows/workflow-syntax-for-github-actions

name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel any in-progress run for the same branch when a new push arrives,
# avoiding redundant work on fast-moving PR branches.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege floor for the whole workflow: grant nothing by default and let
# each job opt into exactly the scopes it needs.
permissions: {}

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # checkout is the only step that uses GITHUB_TOKEN, and it only reads the
    # repo. The cache and artifact-upload steps authenticate with the Actions
    # runtime token (ACTIONS_RUNTIME_TOKEN), not GITHUB_TOKEN — so contents:read
    # is the complete set of scopes this job needs.
    permissions:
      contents: read
    env:
      # Preserve ANSI colour codes in cargo output for readable Actions logs.
      CARGO_TERM_COLOR: always
    steps:
      # Every action is pinned to a full commit SHA (supply-chain hardening); the
      # trailing comment records the human-readable version Dependabot keeps fresh.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          # CI never pushes; don't persist the token in .git/config.
          persist-credentials: false

      # SHA pins the action; `toolchain` selects the rustc version, kept in sync
      # with rust-toolchain.toml so CI and local builds share one rustc. Bump the
      # `toolchain` value (not the SHA) when changing the Rust version.
      - uses: dtolnay/rust-toolchain@67ef31d5b988238dd797d409d6f9574278e20537 # master @ 2026-06-20
        with:
          toolchain: "1.96.0"
          components: rustfmt, clippy

      # Cache registry/git/target keyed by OS + Rust version + Cargo.lock.
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1

      # Single fast installer for every non-rustc tool the just recipes call.
      # If a tool name errors, check the supported list at
      # https://github.com/taiki-e/install-action and adjust that one name.
      - uses: taiki-e/install-action@ace6ebe54a6a0c86dfb5f7764b17f793b6925bc3 # v2.82.3
        with:
          tool: just,cargo-nextest,cargo-deny,cargo-machete,taplo-cli,typos-cli,shellcheck

      # actionlint and gitleaks ship as Go binaries on GitHub Releases. Pin the
      # version and verify the SHA-256 so a tampered or moved asset fails closed.
      - name: install actionlint + gitleaks
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.local/bin"
          install_pinned() {
            name="$1"; url="$2"; sha="$3"
            tgz="$RUNNER_TEMP/$name.tar.gz"
            curl --proto '=https' --tlsv1.2 -sSfL "$url" -o "$tgz"
            echo "$sha  $tgz" | sha256sum --check --status
            tar -xzf "$tgz" -C "$HOME/.local/bin" "$name"
          }
          install_pinned actionlint \
            https://github.com/rhysd/actionlint/releases/download/v1.7.12/actionlint_1.7.12_linux_amd64.tar.gz \
            8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8
          install_pinned gitleaks \
            https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz \
            551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      # One command — identical to `just ci` run locally and by the pre-push hook.
      - name: ci
        run: just ci

      # `just ci` runs `cargo doc`; publish the generated HTML.
      - name: upload docs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: docs
          path: target/doc
          retention-days: 7

  # Verify that the codebase compiles on the declared minimum supported Rust
  # version. rust-version = "1.90" in Cargo.toml is metadata only — without
  # this job, accidentally using a post-1.90 API would go undetected until a
  # downstream user reports a compile error.
  msrv:
    name: MSRV (1.90)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      CARGO_TERM_COLOR: always
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@67ef31d5b988238dd797d409d6f9574278e20537 # master @ 2026-06-20
        with:
          toolchain: "1.90"

      # Cache keyed separately from the `ci` job because of the different toolchain.
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          key: msrv

      - uses: taiki-e/install-action@ace6ebe54a6a0c86dfb5f7764b17f793b6925bc3 # v2.82.3
        with:
          tool: just

      - name: msrv
        run: just msrv
